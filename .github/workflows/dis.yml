name: Dispatched
on:
  check_run:
    types: [requested_action]

jobs:
  testJob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Sleep for 60 seconds
        run: sleep 15s
        shell: bash
